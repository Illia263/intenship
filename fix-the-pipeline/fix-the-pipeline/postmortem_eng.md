| # | What I found | How it would manifest in prod | Why it happened | Fix | Regress test |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |
| 1 | The reason of discrepancy in money | The revenue would stably diverge by n amount, because of this the company's accountants could have problems because of not correct reports | Due to the fact that the intern used Double Precision and float for money, they write the number into binary code with a minimal error, because of this on a distance there will be a small discrepancy in revenue | In the file schema.sql replaced the type of column total to Numeric, in the file etl.py replaced the type of column total from float()  to decimal() | Conducted test through tests/inc3.py, also checked through make check-money |
|  |  |  |  |  |  |
| 2 | The same discrepancy as in the incident with money, but now already with the column dist | At calculating in general how many kilometers was driven there would be not an exact figure | Because of the same reason, the trip distance is written through float() and double precision, at translating into binary code again a discrepancy would be drawn out | Decided not to fix this incident, because there is no sense from this, in any case it is almost impossible to calculate the exact distance of the trip, because the driver can drive a little more or a little less by a couple of meters, that's why the discrepancy which will be caused because the wrong data types were used will not be so significant | Tests were not conducted, because there was no fix |
|  |  |  |  |  |  |
| 3 | Impossibility to extract and load data for past months | It would be impossible to perform analytics for previous periods due to the fact that into the command python etl.py it was impossible to write a date, to extract information from the file for example for 2024-02 or for the time period 2024-01:2024-03. Because of this the script could read the file only which has in itself the date, which was at the moment of its launch | Due to the fact that in the file etl in the function run() it was written, so that information from the file would be thrown in for the date which was in the moment of calling the command in the terminal, due to the fact that now is 2026 year and 7 month, the script tried to read the file with information for July 2026. | Into the file etl.py imported the library argparse and wrote in the function run() a condition, that now when someone will want to launch python etl.py in the terminal, then he will be forced to write additionally --month or --backfill in order for the script to understand for which exact date or for which time period it needs to take files and perform actions over them | Checked through the terminal, once at launching the command python etl.py it was written, that loaded 0 rows. Now when I write additionally for example --month 2024-01 or --backfill 2024-01:2024-12, then are loaded already all files by 500 thousand rows each. Also I checked this with my tests (tests/inc2.py) in the first function we test whether the parser will let through our input without a specified concrete date, if return code > 0, this means, that the parser worked correctly and didn't let to execute the command python etl.py without --month or --backfill. In the second function we check, what will happen if to write everything correctly, python etl.py --month 2024-01, then our return code will be equal to 0, this means, that our function successfully worked and it is indeed correct. Now we conduct the exact same test for python etl.py --backfill 2024-01:2024:12 , return code is also 0 - means everything also works. |
|  |  |  |  |  |  |
| 4 | Vulnerability to sql injection | An attacker could mask an sql injection under simple information which they throw into the database, this query would execute, for example they would mask DROP TABLE trips; --, then the dashboard would break and all information from the database would also be deleted | The intern through python glued and formatted the string and sent it to the library psycopg2 and db, they seeing already ready beautiful string simply executed it, because of this if into the string was sewn an sql injection, they would without questions execute it | Replaced % to a comma between the query and the tuple with data, also removed the quotes around the second %s, because now the code doesn't format itself through python operators and ready is passed for execution, but now we simply pass 2 arguments to the library psycopg2 and she already herself adds 2 arguments and formats them. | In the folder tests made a file sql_injection.py, in it I tried to toss an sql injection which tries to pass the command DROP TABLE under the guise of data, therefore this test checks whether psycopg2 will be able to neutralize the injection without falling with a syntax error. |
|  |  |  |  |  |  |
| 5 | Not idempotent code, the database duplicates with every reload. Also duplicates in the database | As is described in the 1 incident, at reloading the database, the data would double, because of this it would be hard to distinguish which data are real, and which are duplicates | Because the intern wrote not idempotent code. For example was loading some file into the database during which happened some failure, the on-duty for example, restarted the script, and he again duplicated this file, because of this the file practically tripled, because the on-duty his 3 times restarted | In the file schema.sql was added the row UNIQUE(vendor, pickup, dist), now the data will not repeat, because the chance of that, that in 2 different trips will be identical vendor, pickup, dist practically equals 0. Also into the file etl.py was added the query ON CONFLICT ... DO NOTHING and now when db at the query sees, that such a row was already loaded, she simply ignores this row and continues further. To clean from duplicates the db and so that she found out about the file schema.sql I simply used make reset and then again already threw in all files(not sure, if this is a correct decision, maybe it was needed to do somehow differently). | First of all checked through the terminal, with the command make check-idempotent. Also checked with a test (tests/inc1.py). Checked whether the rows will not double, wrote a script which in the terminal writes a query for loading the file through etl.py, and then counts the quantity of all data in the db, in the same function are conducted once again the exact same actions and at the end is compared the 1 calculation with the 2 calculation, if they are equal, then the code is 0 and the test is successfully passed. |
|  |  |  |  |  |  |
| 6 | There was no separate file where all broken, rejected rows would be folded | It would be impossible to conduct analytics, why that or another row was rejected and not thrown into the database | All broken rows were thrown simply into the list rejects and were not outputted anywhere | In the file etl.py slightly perfected the throwing of problematic rows into the list, now the row is thrown together with the reason of why it is rejected. Further if the list of broken rows is not empty the following is executed : A new file dead-letter.ndjson is created and every broken file in the list with broken files is thrown into the newly created dead-letter.ndjson | Made a test(tests/dead_letter_test.py, function test_dead_letter()) which through the library subprocess launches a terminal command which drives through the pipeline a broken file, then checks whether dead-letter.ndjson was created , if yes, then checks whether it is not empty, if not empty, then the test is passed and this test file is deleted |
| 7 | There was no such thing, that if in a file more than 5% of broken rows, then it is not thrown in | Data for the month would pour in not fully, but nobody would notice any error, analysts seeing, that the quantity of trips sharply fell, would be also surprised because of what it happened so, but in reality this all would be due to the fact that the file was not thrown in fully | Due to the fact that the intern in principle didn't write this moment in the code | in etl.py we need to divide the length of the list with broken rows, divide by the quantity of rows, if in the result of dividing the number will be bigger than 0.05 we fall and clean after ourselves all data and also we write the rejected rows into dead letter with reason | Test (tests/dead_letter_test.py, function test_five_percent) in it we connect to the database, we check how many rows at the beginning, so that later to check whether new rows will not add after we fall. Inside we execute a command for execution of a broken file with data, if return code == 1 the test is passed, and we check how many rows at the end remained, if at the beginning and at the end is equally of rows, means cleaning after oneself also works |
|  |  |  |  |  |  |
| 8 | The intern wrote code which spends too much memory | OOM killer would stop the whole process of etl of a file or files into the database, due to the fact, that the memory would exhaust | Due to the fact, that the code at first read the files, then made for them a separate list, threw them there, then from this list scattered the files again between the lists good and rejects, this took very much memory | Made it so, that now is not made another extra list on the stage of reading the file, also rows are not scattered into the lists rejects and good, but now due to the fact that this all is done through a generator, yield processes by one row and marks it either good or bad and deletes this row from memory thereby saving it | Wrote my test(tests/memory_test.py) in it subprocess executes the command --backfill for a whole year in the terminal, if the limit of memory was not exceeded - then the test is passed. Also executed the test through make check memory |
|  |  |  |  |  |  |
| 9 | There were no logs with run id | It would be impossible to see some errors, for example, if a query into the database did not pass successfully, or the script broke still on the way before it managed to throw the row with the error into dead letter. Also it would be impossible to conduct analytics and look whether for example the file for a new month was thrown into the database | Simply the intern in principle did not add this | In the file etl.py used logging, structlog and contextvars. From the very beginning I in general configured logs adding to them structlog.contextvars.merge_contextvars, when in some function will be called for example structlog.info, then merge_conflicts intercepts the call and searches whether there are no any changes there and merges the updated informationos.path.getsize() into the general list of the current log, then I added the level of importance of logs, then added the exact time when the whole process is happening, then after settings I launched the logger. Then in every function is called the logger he creates a json which he fills with data | Made a simple test on logs(tests/los.py) subprocess executes the command python etl.py --month 2024-05 into the terminal, if len(result.stdout) > 0, means the logs after execution of the command are not empty and everything works well |
|  |  |  |  |  |  |
| 10 | Exit codes: 0, 1, 2 were not written, also the code was vulnerable to HTTP errors | At any http error the script would not even try to reconnect to the server. The script could fall, with an ununderstandable traceback  and because of the absence of exit codes the system would not understand, whether the script fell because of an error with connection to the server or this is some problem with data | Were not written neither exit codes nor protection from http errors | Now when the script cannot reach the end, from him will be a clear answer - codes 0, 1, 2. Also if he stumbles upon an HTTP error as an answer to the error will pop up exit code 2. Also I added the library tenacity, namely retry now there will be a repeated attempt of connection to the server if will crawl out one of HTTP errors. This was done exactly the same as they do it in Amazon. Repeated attempts will be executed maximum 3 times with different period in time - maximum 60s | Conducted test(tests/exit_codes.py) with the help of library subprocess executed commands into terminal, to call code 2 I through subprocess executed in the terminal the command python etl.py without --month or --backfill, if return code  == 2 - the test is passed successfully. Second test - test for code 1. I execute command into terminal python etl.py --month 2024-06-broken - in this file more than 5 percent of broken rows, therefore it should give out code 1 and not throw into db this file - if return code == 1 - the test is passed successfully. The last test was test for code 0. Just the same as in the two past cases I through subprocess executed a command in the terminal, this time python etl.py --month 2024-06 , this is not a broken file and in it is already written --month therefore return code will be 0 and the test will be also passed. The last test tests exactly for HTTP error, through subprocess is executed a command for throwing a file into the db which simply doesn't exist python etl.py --month 2024-13, the script should give back 404 Not Found, then tenacity will try 3 times to execute the query to the server, this is also checked in the test we count how many times the logger wrote 'download_started' in result.stdout through result.stdout.count('download_started'), if after 3 attempts it still didn't work out to connect to the server - the test is successful |
|  |  |  |  |  |  |
| 11 | In requirements.txt there was no library pytest and others | In prod - nohow, but for a person who would work with the pipeline after the intern it would be a bit not comfortable, because they would have to manually download libraries which were not added into requirements | The intern didn't add pytest in requirements.txt | Added all new libraries which I used pip freeze > requirements.txt | I think, that a test for such a change is not needed |
|  |  |  |  |  |  |
| 12 | Not optimized speed | The script would work very long, for example --backfill for one year would take +- 20min | Because the intern used a for loop, took by one row, then through INSERT this 1 row threw into the database, because of this the script had to execute this action 500 000 times just for 1 month. | For a start I created a temporary file temp_csv into which will be thrown the rows which yield marked as good, because COPY can copy only from files into a table or vice versa. through writer.writerow are made 4 columns: vendor, pickup, dist, total then writes them through a comma in csv format. Then already with the open file temp_csv We create a temporary table, then with the help of the library psycopg2 we call cope_expert() and inside we copy all data from the file temp_csv into temp_trips, then from the temporary table already simply all at once good rows are passed for one INSERT into our already real table trips (When I optimized the speed, appeared a new problem, I started a discrepancy in money, which I so could not fix, if on a real project it would be needed to choose to leave the correct calculation of money or faster work of the script, I would choose the correct calculation of money, in this situation I decided to leave the optimized speed, because it was harder to do, than to fix the money) | Made test(speed_test) in it made a function with a test in which turned on a timer, through subprocess executed python etl.py --month 2024-03 if it lasts less than 60 seconds - the requirement about speed is fulfilled |
|  |  |  |  |  |  |
| 13 | At throwing one file into db, came some extra ones | There would be a confusion with data in the db | Most likely such data got accidentally not there because of some system failure or human error | In the function with the generator before the block with try we write an if, if the date of the row starts with the date which is not in the variable target_month, then his yield will mark as bad and will throw him into dead-letter | Test(tests/data_test.py) in it I launched python etl.py --month 2024-01 through subprocess, then through psycopg2 is executed an sql query  which checks when a person was picked up and in which year. Then with the help of fetchall() we pull out all the data we need and write into a variable. In the result we check whether the length of the variable is not bigger than 1, if not bigger - the test is passed |
